# Aetheris CI: build, vet, test, lint, and coverage on push and pull_request.
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  actionlint:
    name: Actionlint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lint GitHub Actions workflows
        uses: reviewdog/action-actionlint@v1

  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    env:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.1"
          cache-dependency-path: go.sum

      - name: Build
        run: go build -v ./...

      - name: Vet
        run: go vet ./...

      - name: Check gofmt
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "The following files are not formatted with gofmt:"
            gofmt -l .
            exit 1
          fi

      - name: Run unit tests
        run: go test -v -race -count=1 -short ./...

      - name: Test coverage (atomic mode)
        run: go test -coverprofile=coverage.out -covermode=atomic ./...

      - name: Check coverage threshold (overall >= 30%)
        run: |
          COVERAGE=$(go tool cover -func=coverage.out | awk '/^total:/{gsub("%", "", $3); print $3}')
          if [ -z "$COVERAGE" ]; then
            echo "ERROR: Failed to parse total coverage from coverage.out"
            exit 1
          fi
          echo "Overall Coverage: $COVERAGE%"
          
          # Check core module coverage (internal/runtime, internal/agent, internal/pipeline)
          echo ""
          echo "=== Core Module Coverage ==="
          go tool cover -func=coverage.out | grep -E "(internal/runtime|internal/agent|internal/pipeline)" | head -20
          
          # Overall threshold: 30%
          if ! awk -v c="$COVERAGE" 'BEGIN { exit !(c >= 30.0) }'; then
            echo "ERROR: Overall code coverage is below 30%"
            exit 1
          fi
          echo ""
          echo "✓ Coverage check passed (>= 30%)"

      - name: Upload coverage to Codecov
        if: ${{ env.CODECOV_TOKEN != '' }}
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.out
          flags: unittests
          fail_ci_if_error: true
          token: ${{ env.CODECOV_TOKEN }}

      - name: Skip Codecov upload (no token)
        if: ${{ env.CODECOV_TOKEN == '' }}
        run: echo "Skipping Codecov upload because CODECOV_TOKEN is not configured"

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.1"
          cache-dependency-path: go.sum

      - name: Install golangci-lint (build with Go 1.26.1)
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest

      - name: Run golangci-lint
        run: $(go env GOPATH)/bin/golangci-lint run ./... --timeout=5m --config=.golangci.yml

  postgres-integration:
    name: Postgres Integration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: aetheris
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d aetheris"
          --health-interval 5s
          --health-timeout 10s
          --health-retries 20
    env:
      TEST_JOBSTORE_DSN: postgres://postgres:postgres@localhost:5432/aetheris?sslmode=disable
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.1"
          cache-dependency-path: go.sum

      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Wait for PostgreSQL readiness
        run: |
          for i in $(seq 1 30); do
            if pg_isready -d "$TEST_JOBSTORE_DSN" >/dev/null 2>&1; then
              echo "PostgreSQL is ready"
              exit 0
            fi
            echo "Waiting PostgreSQL... ($i/30)"
            sleep 2
          done
          echo "ERROR: PostgreSQL is not ready in expected time"
          exit 1

      - name: Apply schema
        run: psql "$TEST_JOBSTORE_DSN" -f internal/runtime/jobstore/schema.sql

      - name: Run Postgres-backed tests
        run: go test -v ./internal/runtime/jobstore ./internal/agent/job

  sdk-go:
    name: SDK Go (durability)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdk/durability
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.1"
          cache-dependency-path: sdk/durability/go.sum

      - name: Build
        run: go build -v ./...

      - name: Vet
        run: go vet ./...

      - name: Check gofmt
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "The following files are not formatted with gofmt:"
            gofmt -l .
            exit 1
          fi

      - name: Run tests with race detector
        run: go test -v -race -count=1 ./...

      - name: Verify no main module dependency
        run: |
          if go mod graph | grep -q "Aetheris/v2"; then
            echo "ERROR: SDK depends on main Aetheris module (violates zero-dep constraint)"
            go mod graph | grep "Aetheris/v2"
            exit 1
          fi
          echo "✓ SDK has no dependency on main Aetheris module"

  sdk-python:
    name: SDK Python (durability-py)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]
    defaults:
      run:
        working-directory: sdk/durability-py
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint (flake8)
        run: |
          pip install flake8
          flake8 aetheris_durability/ --max-line-length=120 --extend-ignore=E203,W503 || true

      - name: Run tests
        run: python -m pytest tests/ -v

  coverage-report:
    name: Coverage Report
    needs: [actionlint, build-and-test, lint, postgres-integration, sdk-go, sdk-python]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.1"
          cache-dependency-path: go.sum

      - name: Generate coverage report
        run: |
          go test -coverprofile=coverage.out -covermode=atomic ./...
          
      - name: Display coverage summary
        run: |
          echo "=== Coverage Summary by Package ==="
          go tool cover -func=coverage.out | grep total:
          
          echo ""
          echo "=== Packages with lowest coverage (top 10) ==="
          go tool cover -func=coverage.out | grep -v total: | sort -t$'\t' -k3 -n | head -10

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.out
          retention-days: 30
